language: objective-c
xcode_workspace: BIObjCHelpers.xcworkspace
osx_image: beta-xcode6.4

before_install:
  - brew update
  - if brew outdated | grep -qx xctool; then brew upgrade xctool; fi
  - sudo easy_install cpp-coveralls

script:
  - xctool test -workspace BIObjCHelpers.xcworkspace -scheme BIObjCHelpers -sdk iphonesimulator8.4 -destination "platform=iOS Simulator,name=iPhone 6,OS=8.4"

after_success: 
  ./coveralls.rb --exclude-folder ./Example --exclude-folder ./Pods
